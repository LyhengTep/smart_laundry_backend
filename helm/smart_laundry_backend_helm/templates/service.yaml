apiVersion: v1
kind: Service
metadata:
  name: {{ include "smart_laundry_backend_helm.fullname" . }}
  labels:
    {{- include "smart_laundry_backend_helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "smart_laundry_backend_helm.selectorLabels" . | nindent 4 }}
